import { CustomAlertRoot } from './CustomAlertRoot'
import { CustomAlertTitle } from './CustomAlertTitle'
import { CustomAlertButtonContainer } from './CustomAlertButtonContainer'

export const CustomAlert = {
  Root: CustomAlertRoot,
  Title: CustomAlertTitle,
  ButtonContainer: CustomAlertButtonContainer,
}
